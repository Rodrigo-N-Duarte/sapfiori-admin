sap.ui.define([], function () {
    "use strict";

    var Formatter = {
      
    };

    return Formatter;

}, true);

// Importar dentro do controller com namespace/Formatter e nos parâmetros da função
// Criar atributo global no formatter => objFormatter: Formatter,
// Importar formatter na view como: .objFormatter.nomeDaFuncao
